toml
[versions]# ... other versions
# Ensure this version matches the first part of your Kotlin version
ksp = "2.0.21-1.0.27"

[plugins]
# ... other plugins
ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }